#!/usr/bin/env python3
"""
Description:
Function that takes
a string (str1) and a
string (str2) as arguments
and returns a concatenated
string.
"""


def concat(str1: str, str2: str) -> str:
    """
    Takes two strings and
    returns them as a
    concatenated string.
    """

    return str1 + str2
